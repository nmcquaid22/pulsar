{% extends "@pulsar/pulsar/components/tab.html.twig" %}

{% block tab_content %}
<div class="modal modal__example" style="display: block">
        <div class="modal__dialog">
          <div class="modal__content">
            <div class="modal__header">
              <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
              <h4 class="modal__title">A simple example</h4>
            </div>
            <div class="modal__body">

<div class="nav-inline">
        <ul class="nav-inline__list">
            <li class="nav-inline__item">
            {{
                html.link({
                    'label': 'Standard',
                    'class': 'nav-inline__link',
                    'href': '#standard',
                    'data-toggle': 'tab'
                })
            }}
            </li>
            <li class="nav-inline__item">
            {{
                html.link({
                    'label': 'Logic',
                    'class': 'nav-inline__link',
                    'href': '#logic',
                    'data-toggle': 'tab'
                })
            }}
            </li>
            <li class="nav-inline__item is-active">
            {{
                html.link({
                    'label': 'Questions',
                    'class': 'nav-inline__link',
                    'href': '#questions',
                    'data-toggle': 'tab'
                })
            }}
            </li>
        </ul>
    </div>

<table class="table datatable table--full" data-selection="false">
        <thead>
            <tr>
                <td class="table-responsive"></td>
                <td class="table-selection"></td>
                <th><a href="#">Page</a></th>
                <th><a href="#">Section</a></th>
                <th><a href="#">Question</a></th>
                <th><a href="#">Response</a></th>

            </tr>
        </thead>
        <tbody>

            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Main section</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' First Name', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>
            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Main section</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' Last Name', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>
            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Main section</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' Email address', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>




            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Your address</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' House number', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>
            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Your address</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' Street name', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>
            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Your address</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' City', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>
            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Your address</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' Country', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>
            <tr>
                <td class="table-responsive">{{ html.icon('plus-sign', { 'class': 'table-child-toggle' }) }}</td>
                <td class="table-selection">{{ html.icon('unchecked', { 'class': 'table-row-select js-select' }) }}</td>
                <td>Your details</td>
                <td>Your address</td>
                <td>{{ html.link({ 'label': html.icon('plus-sign') ~ ' Postcode', 'style': 'display: block' }) }}</td>
                <td>
                    {{ html.link({ 'label': html.icon('plus-sign') ~ '', 'style': 'display: block' }) }}
                </td>
            </tr>

        </tbody>
    </table>


            </div>
            <div class="modal__footer">
              <button type="button" class="btn" data-dismiss="modal">Cancel</button>
              <button type="button" class="btn btn--primary">Save Changes</button>
            </div>
          </div><!-- /.modal__content -->
        </div><!-- /.modal__dialog -->
      </div><!-- /.modal -->

{% endblock %}
